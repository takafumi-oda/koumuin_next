<h1>トップページ</h1>
<table border=1>
  <tr>
    <th>タイトル</th>
    <th>投稿時間</th>
  </tr>
  <% @posts.each do |post| %>
    <tr>
      <td><%= link_to "#{post.title}", post_path(post.id) %></td>
      <td><%= datetime_format(post.created_at) %></td>
    </tr>
  <% end %>
</table>
